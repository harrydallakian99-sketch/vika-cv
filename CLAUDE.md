# Վիկա Գյոզալյան — ինտերակտիվ CV («Orbit»)

Սա իմ (Վիկայի) անձնական CV-կայքն է՝ «թռիչք տիեզերքում» ոճով։ Մեկ HTML ֆայլ, առանց build-ի, առանց գրադարանների։
Կայքը բաց է այստեղ՝ https://vika.77-233-220-78.sslip.io/

## Լեզու
Ինձ հետ խոսիր **հայերեն**։ Կարճ, առանց ավելորդ բացատրությունների։ Կոդում մեկնաբանությունները կարող են մնալ անգլերեն։

## Կառուցվածք

```
vika-cv/
├── CLAUDE.md        <- այս ֆայլը
├── index.html       <- ամբողջ կայքը (CSS + HTML + JS մեկ ֆայլում)
├── assets/          <- վիդեոներ (720p, mp4) և պոստերներ (jpg)
├── qr/              <- QR-կոդեր դեպի կայք (png/svg)
└── deploy.py        <- Գարիկի սկրիպտն է, ինձ պետք չէ (տես «Հրապարակում»)
```

### index.html-ի ներսում
- **5 տեսարան** (`<section class="slide">`)՝ Մեկնարկ · Իմ մասին · Հմտություններ · Աշխատանքներ · Կապ։
- **Լեզուներ՝ ՀԱՅ / RU / EN։** Հայերենը լռելյայն է։
  - Ռուսերենը գրված է ուղիղ HTML-ի մեջ։
  - Անգլերենը՝ `data-en="..."` ատրիբուտում նույն էլեմենտի վրա։
  - Հայերենը՝ JS-ի `HY` բառարանում, բանալին անգլերեն տեքստն է՝ `'English text':'Հայերեն տեքստ'`։
  - **Կանոն.** ցանկացած նոր տեքստ = 3 տեղ՝ ռուսերեն HTML-ում + `data-en` + տող `HY`-ում։
- **Աշխատանքները**՝ «Աշխատանքներ» տեսարանում, `.reel-strip`-ի ներսում, ամեն մեկը `<button class="tile ...">`։
- **Կատեգորիաներ** (`data-cat`)՝ `shoot` նկարահանում · `aiv` AI-վիդեո · `edit` մոնտաժ · `aip` AI-ֆոտո։ Ֆիլտրի կոճակները վերևում են։

## Ինչպես ավելացնել նոր աշխատանք

1. Վիդեոն սեղմել (բնօրինակը մեծ է, կայքի համար պետք է 720p, ~1 ՄԲ/10 վրկ).
   ```bash
   ffmpeg -i ԲՆՕՐԻՆԱԿ.mov -vf "scale=720:-2" -c:v libx264 -crf 27 -maxrate 1400k -bufsize 2800k -preset slow -pix_fmt yuv420p -movflags +faststart -c:a aac -b:a 96k assets/ԱՆՈՒՆ.mp4
   ```
2. Պոստեր (առաջին կադրը).
   ```bash
   ffmpeg -ss 1 -i ԲՆՕՐԻՆԱԿ.mov -frames:v 1 -vf "scale=540:-2" -q:v 4 assets/ԱՆՈՒՆ.jpg
   ```
3. Լուսանկարի համար՝ պարզապես jpg դնել `assets/`-ում (մինչև ~400 ԿԲ, `sips -s formatOptions 82`).
4. `index.html`-ում `.reel-strip`-ի մեջ ավելացնել սալիկ.
   ```html
   <button class="tile tile--v" data-cat="aiv" data-src="assets/ԱՆՈՒՆ.mp4" aria-label="Հաճախորդ — AI video">
     <video muted loop playsinline preload="none" poster="assets/ԱՆՈՒՆ.jpg" src="assets/ԱՆՈՒՆ.mp4"></video>
     <span class="cap"><b data-en="AI video">ИИ-видео</b><span data-en="Client · what">Клиент · что</span></span>
   </button>
   ```
   Լուսանկարի համար՝ `class="tile tile--p"`, ներսում `<img src="assets/ԱՆՈՒՆ.jpg" alt="" loading="lazy">`։
5. Ստորագրության հայերենն ավելացնել `HY` բառարանում՝ `'Client · what':'Հաճախորդ · ինչ',`։

## Ինչպես ստուգել տեղում
```bash
python3 -m http.server 8080
```
և բացել http://localhost:8080 — ուղիղ ֆայլը (file://) բացելիս վիդեոները կարող են չխաղալ։
Ստուգել՝ բոլոր 3 լեզուներով, հեռախոսի լայնությամբ, որ ֆիլտրն աշխատում է, վիդեոն բացվում է ամբողջ էկրանով։

## Հրապարակում
Կայքը գտնվում է Գարիկի սերվերի վրա։ Փոփոխությունները commit + push անել GitHub, հետո գրել Գարիկին՝ նա մեկ հրամանով կհրապարակի։
`deploy.py`-ը չաշխատեցնել՝ այն պահանջում է սերվերի մուտք, որն այստեղ չկա։

## Ինչ ՉԱՆԵԼ
- Չփոխել տեսարանների անցումների լոգիկան (`go()`, `warp`, canvas աստղեր), եթե հատուկ չեմ խնդրել։
- Չավելացնել արտաքին գրադարաններ ու fonts, բացի Google Fonts-ից։
- Չդնել `assets/`-ում 720p-ից մեծ կամ 8 ՄԲ-ից ծանր վիդեո։
- Չհեռացնել `data-en`-ը և `HY`-ի տողերը՝ առանց դրանց լեզուները կփչանան։
- Չհրապարակել ուրիշների աշխատանքներ առանց հաճախորդի համաձայնության։

## Կոնտակտներ կայքում
Telegram @vika_gyozalyan · Instagram @vvvikas_blog · +374 93 660 683 — փոխելիս թարմացնել «Կապ» տեսարանում։
